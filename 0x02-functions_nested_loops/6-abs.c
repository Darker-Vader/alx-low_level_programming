#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer
 *@a: function parameter
 * Return: 0 (success)
 */
int _abs(int a)
{

	if (a < 0)
		return (-a);
	else
		return (a);
}
